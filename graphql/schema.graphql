type Query {
  fetchMission(missionID: Int!): Mission
  fetchMissions: [Mission]
}

###############################################################################
type Mission {
	id: Int
	point: Int
	detail: String
}
