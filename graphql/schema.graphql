type Query {
  fetchMission(missionID: Int!): Mission
  fetchMissions: [Mission]
  fetchTxCount(sender: String!): TxCount
}

###############################################################################
type Mission {
	id: Int
	point: Int
	detail: String
}

type TxCount {
  total_txs: Int
}
